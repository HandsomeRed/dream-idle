# 技术指标计算实现

**创建时间：** 2026-03-13 00:15  
**更新时间：** 2026-03-13 00:15  
**分类：** 量化交易  
**标签：** #技术指标 #MA #MACD #RSI #布林带 #Python  
**来源：** 实践  
**状态：** 已固化 ⭐⭐⭐⭐⭐

---

## 📖 核心概念

技术分析常用指标的实现方法，基于 Pandas 计算。

---

## 🔧 指标实现

### 1. 移动平均线 (MA)

```python
def calculate_ma(df: pd.DataFrame, periods: list = [5, 10, 20, 60]) -> pd.DataFrame:
    """计算移动平均线"""
    df = df.copy()
    for period in periods:
        df[f'MA{period}'] = df['收盘'].rolling(window=period).mean()
    return df
```

**说明：**
- MA5：5 日均线，短期趋势
- MA10：10 日均线，短期趋势
- MA20：20 日均线，中期趋势（月线）
- MA60：60 日均线，长期趋势（季线）

### 2. 指数移动平均线 (EMA)

```python
def calculate_ema(df: pd.DataFrame, periods: list = [12, 26]) -> pd.DataFrame:
    """计算指数移动平均线"""
    df = df.copy()
    for period in periods:
        df[f'EMA{period}'] = df['收盘'].ewm(span=period, adjust=False).mean()
    return df
```

**说明：**
- EMA12：快线，用于 MACD
- EMA26：慢线，用于 MACD

### 3. MACD 指标

```python
def calculate_macd(df: pd.DataFrame, 
                   fast: int = 12, 
                   slow: int = 26, 
                   signal: int = 9) -> pd.DataFrame:
    """计算 MACD 指标"""
    df = df.copy()
    
    # 计算 EMA
    ema_fast = df['收盘'].ewm(span=fast, adjust=False).mean()
    ema_slow = df['收盘'].ewm(span=slow, adjust=False).mean()
    
    # MACD 线 = 快线 EMA - 慢线 EMA
    df['MACD'] = ema_fast - ema_slow
    
    # 信号线 = MACD 的 EMA
    df['Signal'] = df['MACD'].ewm(span=signal, adjust=False).mean()
    
    # MACD 柱状图
    df['MACD_Hist'] = df['MACD'] - df['Signal']
    
    return df
```

**信号解读：**
- **金叉：** MACD 上穿 Signal → 买入信号
- **死叉：** MACD 下穿 Signal → 卖出信号
- **柱状图 > 0：** 多头强势
- **柱状图 < 0：** 空头强势

### 4. RSI 相对强弱指标

```python
def calculate_rsi(df: pd.DataFrame, period: int = 14) -> pd.DataFrame:
    """计算 RSI 相对强弱指标"""
    df = df.copy()
    
    # 计算价格变化
    delta = df['收盘'].diff()
    
    # 分离上涨和下跌
    gain = delta.where(delta > 0, 0)
    loss = -delta.where(delta < 0, 0)
    
    # 计算平均涨幅和跌幅
    avg_gain = gain.rolling(window=period).mean()
    avg_loss = loss.rolling(window=period).mean()
    
    # 计算 RS 和 RSI
    rs = avg_gain / avg_loss
    df['RSI'] = 100 - (100 / (1 + rs))
    
    return df
```

**信号解读：**
- **RSI > 70：** 超买区，可能回调
- **RSI < 30：** 超卖区，可能反弹
- **RSI 30-70：** 中性区

### 5. 布林带 (Bollinger Bands)

```python
def calculate_bollinger_bands(df: pd.DataFrame, 
                               period: int = 20, 
                               std_dev: float = 2.0) -> pd.DataFrame:
    """计算布林带"""
    df = df.copy()
    
    # 中轨 = MA
    df['BB_Middle'] = df['收盘'].rolling(window=period).mean()
    
    # 标准差
    std = df['收盘'].rolling(window=period).std()
    
    # 上轨和下轨
    df['BB_Upper'] = df['BB_Middle'] + (std_dev * std)
    df['BB_Lower'] = df['BB_Middle'] - (std_dev * std)
    
    return df
```

**信号解读：**
- **突破上轨：** 超买，可能回调
- **突破下轨：** 超卖，可能反弹
- **布林带收窄：** 即将变盘
- **布林带扩张：** 趋势加强

---

## 📊 完整使用示例

```python
from kline_calculator import calculate_all_indicators
import akshare as ak

# 获取数据
df = ak.stock_zh_a_hist(
    symbol="000001",
    period="daily",
    start_date="20240101",
    end_date="20241231"
)

# 计算所有指标
df = calculate_all_indicators(df)

# 查看结果
print(df[['日期', '收盘', 'MA5', 'MA20', 'MACD', 'RSI', 'BB_Upper']].tail())
```

**输出：**
```
           日期     收盘     MA5  ...    MACD      RSI   BB_Upper
237  2024-12-25  11.92  11.744  ...  0.0865  67.39  11.947437
238  2024-12-26  11.86  11.798  ...  0.0764  58.20  11.965356
239  2024-12-27  11.83  11.840  ...  0.0651  56.45  11.974960
240  2024-12-30  11.95  11.884  ...  0.0712  56.45  12.001459
241  2024-12-31  11.70  11.852  ...  0.0523  48.95  11.997782
```

---

## ⚠️ 注意事项

1. **NaN 值处理** - 均线类指标前期会有 NaN，需要 dropna() 或填充
2. **参数调整** - 不同股票、不同周期可能需要调整参数
3. **指标组合** - 单一指标可靠性低，建议多指标组合
4. **回测验证** - 实盘前必须回测验证

---

## 📊 测试验证

**测试文件：** `tests/test_kline_calculator.py`  
**测试结果：** 11/11 通过 ✅

**测试覆盖：**
- ✅ MA 计算正确性
- ✅ MA 初始 NaN 处理
- ✅ EMA 计算
- ✅ MACD 公式验证
- ✅ RSI 范围验证 (0-100)
- ✅ 布林带关系验证 (上轨≥中轨≥下轨)

---

## 🔗 相关资源

- [K 线计算器代码](/root/.openclaw/workspace/projects/stock-quant/src/data/kline_calculator.py)
- [AKShare 数据获取](./AKShare 数据获取指南.md)
- [均线交叉策略](./均线交叉策略.md)
- [回测引擎架构](./回测引擎架构.md)

---

## 📝 更新记录

| 日期 | 更新内容 | 作者 |
|:---|:---|:---|
| 2026-03-13 | 初始创建 | 虾虾红 |
